---
# inventory/hosts.yml
# Static inventory — replace IPs with your actual host addresses

all:
  children:

    dev_servers:
      hosts:
        dev-web-01:
          ansible_host: 10.0.1.10
          ansible_user: ubuntu
        dev-web-02:
          ansible_host: 10.0.1.11
          ansible_user: ubuntu

    staging_servers:
      hosts:
        staging-web-01:
          ansible_host: 10.1.1.10
          ansible_user: ubuntu

    prod_servers:
      hosts:
        prod-web-01:
          ansible_host: 10.2.1.10
          ansible_user: ubuntu
        prod-web-02:
          ansible_host: 10.2.1.11
          ansible_user: ubuntu

    k8s_control_plane:
      hosts:
        k8s-master-01:
          ansible_host: 10.0.10.10
          ansible_user: ubuntu

    k8s_workers:
      hosts:
        k8s-worker-01:
          ansible_host: 10.0.10.11
          ansible_user: ubuntu
        k8s-worker-02:
          ansible_host: 10.0.10.12
          ansible_user: ubuntu
